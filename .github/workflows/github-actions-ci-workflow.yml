name: lint-frontend-project-lvl1

on: push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js
      uses: actions/setup-node@v1
    - name: Install dependencies
    - run: make install
    - name: Run eslint
    - run: make lint